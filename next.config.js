/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
      domains: [
        "w.namu.la",
        "t1.daumcdn.net",
    ]
  }
}

module.exports = nextConfig
